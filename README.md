# IS601_Final-Project_JG

main.py creates a REST API backend for our dosa restaurant, utilizing the SQLite3 database & FASTAPI. 
API supports CRUD (create, read, update, delete) for each of the DB tables: Orders, Items, Customers.
script named init_db.py will initialize a database named db.sqlite. The DB uses relational constraints (primary keys and foreign keys).

